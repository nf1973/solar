import {
  ResponsiveContainer,
  BarChart,
  CartesianGrid,
  XAxis,
  YAxis,
  Tooltip,
  Legend,
  Bar,
  AreaChart,
  Area,
  LineChart,
} from "recharts";
import { Card, CardHeader, CardContent } from "./ui/card";

const kWhFormatter = (num) => {
  return (num / 1000).toFixed(1);
};

// Format date for axis label
const formatDateforAxisLabel = (date) => {
  const day = date.slice(6, 8);
  return day;
};

const DailySummaryChart = ({ allData }) => {
  // Transform the data to match the expected format for the chart
  const transformedData = allData
    .filter((item) => item.date.startsWith("202312")) // TO DO THIS SHOULD NOT BE HARDCODED TO DECEMBER 2023!!
    .map((item) => ({
      "Total Energy Generated": kWhFormatter(item.energyGenerated),
      date: formatDateforAxisLabel(item.date),
    }));

  return (
    <Card className="w-full">
      <CardHeader>
        <h3 className="text-lg font-bold">
          Energy Generated by Month for December 2023
        </h3>
      </CardHeader>
      <CardContent>
        <ResponsiveContainer width="100%" height={400}>
          <BarChart data={transformedData}>
            <CartesianGrid strokeDasharray="3 3" />
            <XAxis dataKey="date" />
            <YAxis
              type="number"
              domain={[0, 15]} //TODO needs to be dyamic
              label={{
                value: "kWh",
                position: "insideLeft",
                angle: "-90",
                offset: "0",
              }}
            />
            <Tooltip />
            <Legend />
            <Bar dataKey="Total Energy Generated" fill="#0D9488" />
          </BarChart>
        </ResponsiveContainer>
      </CardContent>
    </Card>
  );
};

export default DailySummaryChart;
